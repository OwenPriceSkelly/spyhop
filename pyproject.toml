[project]
name = "spyhop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Owen Price Skelly", email = "21372141+OwenPriceSkelly@users.noreply.github.com" }
]
requires-python = ">=3.13, <3.14"
dependencies = [
    "httpx>=0.27",
    "typer>=0.12",
]

[dependency-groups]
dev = [
    "modal>=1.0",
    "pydantic>=2.0",
]

[project.scripts]
spyhop = "spyhop:main"

[build-system]
requires = ["uv_build>=0.9.5,<0.10.0"]
build-backend = "uv_build"
